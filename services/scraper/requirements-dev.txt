-r requirements.txt
pytest==8.2.2
pytest-asyncio==0.23.7
respx==0.21.1
# python-magic needs the native libmagic library. The Docker image gets it
# via `apt-get install libmagic1`; local Windows dev/test needs this instead.
python-magic-bin==0.4.14; sys_platform == "win32"
