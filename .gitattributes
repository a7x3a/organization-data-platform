# Auto detect text files and perform LF normalization
* text=auto eol=lf

# Force shell scripts and docker entrypoints to LF
*.sh text eol=lf
*.bash text eol=lf
Dockerfile* text eol=lf
*.conf text eol=lf
